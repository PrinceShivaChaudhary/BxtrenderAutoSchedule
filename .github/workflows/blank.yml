name: Master_2nd
on:
  push:
    branches:
      - master
  schedule:
    - cron: '57 18 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Running
      run: |
        echo "Running!"
